import React from "react";

const SearchBar = () => {
  return (
    <input
      className="w-full bg-secondary shadow py-3 px-5 rounded-lg "
      type="text"
      placeholder="Search Your Images..."
    />
  );
};

export default SearchBar;
