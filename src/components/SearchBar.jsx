import React from "react";

const SearchBar = () => {
  return (
    <input
      className="w-full bg-secondary shadow p-3 rounded-md"
      type="text"
      placeholder="Search Your Images..."
    />
  );
};

export default SearchBar;
